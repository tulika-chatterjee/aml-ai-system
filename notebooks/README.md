# Notebooks

Use this folder for:

- Exploratory analysis on exported synthetic tables (never commit real customer data).
- Prototyping new features before promoting them into `models/` or `agents/`.
- Threshold tuning experiments with precision/recall on labelled alert outcomes.

Suggested kernel: Python 3.11+ with `backend/requirements.txt` installed.
