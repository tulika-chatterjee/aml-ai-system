DATABASE_URL=sqlite+aiosqlite:///./aml_intel.db
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Optional — enables LLM Investigator (case narratives). Without this, template text is used.
OPENAI_API_KEY=
# OpenAI model id, e.g. gpt-4, gpt-4o, gpt-4-turbo, gpt-4o-mini (default in config if unset)
LLM_MODEL=gpt-4
