# Subpath deploy (e.g. GitHub Pages at https://user.github.io/aml-ai-dashboard/)
# Leave unset for local dev and Docker (served at /).
# VITE_BASE_PATH=/aml-ai-dashboard

# Production (Vercel): Render API origin — no trailing slash
# VITE_API_URL=https://your-aml-api.onrender.com
