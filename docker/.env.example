# Copy to docker/.env and adjust. Used by: docker compose --env-file docker/.env -f docker/docker-compose.yml up --build

OPENAI_API_KEY=
# LLM_MODEL=gpt-4

# Optional overrides (defaults are set in docker-compose.yml for api service)
# DATABASE_URL=postgresql+asyncpg://aml:aml@postgres:5432/aml_intel
